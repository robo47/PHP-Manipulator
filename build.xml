<?xml version="1.0" encoding="UTF-8"?>
<project name="PHP_CodeFormatter" basedir=".">

    <!-- Clean Project -->
    <target name="clean">
        <delete dir="${basedir}/build"          failonerror="false" />
        <delete dir="${basedir}/tests/build"    failonerror="false" />
        <delete dir="${basedir}/tests/tmp"      failonerror="false" />
        <delete dir="${basedir}/website/api"    failonerror="false" />
    </target>

    <!-- Prepare -->
    <target name="prepare">
        <mkdir dir="${basedir}/build/logs" />
        <mkdir dir="${basedir}/build/docs" />
        <mkdir dir="${basedir}/build/reports" />
        <mkdir dir="${basedir}/tests/tmp" />
    </target>

    <!-- Run php lint check on lib, tests and fixtures -->
    <target name="phplint">
        <parallel threadCount="4" failonany="true">
            <apply executable   = "php"
                   failonerror  = "true">
                <arg value="-nl" />
                <fileset dir="PHP">
                    <include name="**/*.php" />
                </fileset>
            </apply>
            <apply executable   = "php"
                   failonerror  = "true">
                <arg value="-nl" />
                <fileset dir="tests/">
                    <include name="**/*.php" />
                    <include name="**/phtml" />
                </fileset>
            </apply>
        </parallel>
    </target>

    <!-- Run phpcpd -->
    <target name="phpcpd">
        <exec dir="${basedir}"
            executable="phpcpd"
            failonerror="true">
            <arg line="--log-pmd &quot;${basedir}/build/logs/pmd.xml&quot; &quot;${basedir}/PHP&quot;"/>
        </exec>
    </target>

    <!-- Run PDepend -->
    <target name="pdepend">
        <exec dir="${basedir}"
            executable="pdepend"
            failonerror="true">
            <arg line="--jdepend-xml=&quot;${basedir}/build/logs/jdepend.xml&quot; &quot;${basedir}/PHP&quot;"/>
        </exec>
    </target>

    <!-- Run PHP_Codesniffer -->
    <target name="phpcs">
        <exec dir="${basedir}"
            executable="phpcs"
            output="${basedir}/build/logs/checkstyle.xml"
            failonerror="false">
            <arg line="--report=checkstyle --standard=robo47 &quot;${basedir}/PHP&quot;"/>
        </exec>
    </target>

    <!-- Run PHPUnit for libraray -->
    <target name="phpunit">
        <exec dir="${basedir}/tests/"
            executable="phpunit"
            failonerror="true">
            <arg line="--no-configuration
                       --configuration &quot;${basedir}/tests/phpunit.xml&quot;
                       --log-junit &quot;${basedir}/build/logs/junit.xml&quot;
                       --coverage-html &quot;${basedir}/build/logs/coverage/&quot;
                       --coverage-clover &quot;${basedir}/build/logs/clover.xml&quot;" />
        </exec>
    </target>

    <!-- Run PHPUnit for Tests (Constraints, Mocks, TestCase, Util, ... ) -->
    <target name="phpunitTestcode">
        <exec dir="${basedir}/tests/"
            executable="phpunit"
            failonerror="true">
            <arg line="--no-configuration
                       --configuration &quot;${basedir}/tests/phpunitTests.xml&quot;" />
        </exec>
    </target>

    <!-- Run PHP_CodeBrowser -->
    <target name="phpcb">
        <mkdir dir="${basedir}/build/reports/phpcb" />
        <exec executable="phpcb" dir="${basedir}/build/reports/phpcb/">
            <arg line="--log &quot;${basedir}/build/logs/&quot;
        --source &quot;${basedir}/PHP/&quot;
        --output &quot;${basedir}/build/reports/phpcb/&quot;" />
        </exec>
    </target>
    
    <!-- Create pear package -->
    <!-- @todo think about using PHING which has a pear package version 2 task -->
    <target name="package">
        
        <tstamp>
            <format property="pear_package_date" pattern="yyyy-MM-dd"/>
        </tstamp>
        <tstamp>
            <format property="pear_package_time" pattern="HH:mm:ss"/>
        </tstamp>
        <mkdir dir="build/package/PHP"/>

        <copy todir="build/package/PHP/" >
            <fileset dir="${basedir}/PHP/" />
        </copy>

        <copy tofile="build/package/phpmanipulator.php" file="phpmanipulator.php" />

        <exec command="git rev-list --max-count=1 HEAD" outputproperty="pear_package_git_hash" />

        <replace token="@pear_package_version@" value="${packageversion}" dir="build/package/PHP/">
            <include name="**/*.php" />
        </replace>

        <replace token="@pear_package_git_hash@" value="${pear_package_git_hash}" dir="build/package/PHP/">
            <include name="**/*.php" />
        </replace>

        <copy file="package.xml.dist" tofile="build/package/package.xml" overwrite="true" />

        <replace file="build/package/package.xml" token="@pear_package_version@" value="${packageversion}" />
        <replace file="build/package/package.xml" token="@pear_package_date@" value="${pear_package_date}" />
        <replace file="build/package/package.xml" token="@pear_package_time@" value="${pear_package_time}" />
        <replace file="build/package/package.xml" token="@pear_package_git_hash@" value="${pear_package_git_hash}" />

        <exec executable="pear" dir="${basedir}/build/package/">
            <arg line="package" />
        </exec>

        <copy tofile="PHP_Manipulator-${packageversion}.tgz" file="build/package/PHP_Manipulator-${packageversion}.tgz" />
    </target>

    <!-- Run Sloccount -->
    <target name="sloccount">
        <mkdir dir="${basedir}/build/logs/sloccount/" />
        <exec executable="sloccount"
              dir="${basedir}"
              output="${basedir}/build/logs/sloccount.sc"
              failonerror="true">
            <arg line="--duplicates --wide --details --addlangall --datadir &quot;${basedir}/build/logs/sloccount/&quot; &quot;${basedir}/PHP/&quot;"/>
        </exec>
    </target>

    <!-- Build complete Project -->
    <target name="build" depends="clean, prepare" >
        <antcall target="phplint" />
        <parallel threadCount="4" failonany="true">
            <antcall target="phpunit" />
            <antcall target="phpcs" />
            <antcall target="phpcpd" />
            <antcall target="pdepend" />
            <antcall target="sloccount" />
            <antcall target="phpunitTestcode" />
        </parallel>
        <antcall target="phpcb" />
    </target>

</project>
